---
# The variables file used for Timezone playbooks.
# If not overriden in inventory or as a parameter, this is the value that will be used

# Locales to generate (The first one will be the default)
common_locales:
  - en_US.UTF-8
